apiVersion: v1
kind: Service
metadata:
  name: echo-server-service
  labels: 
    name: echo-server-service
    app: echo-server-app
spec:
  type: LoadBalancer
  ports:
  - port:  800
    targetPort: 5000
  selector:
    name: echo-server-pod
    app: echo-server-app